use_default_settings: true

server:
  secret_key: "fetchmark-dev-do-not-use-in-prod"
  limiter: false
  image_proxy: false
  bind_address: "0.0.0.0"
  port: 8080

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json
